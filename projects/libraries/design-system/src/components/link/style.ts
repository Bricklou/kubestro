import { tv } from 'tailwind-variants'

export const linkVariants = tv({
  base: 'text-link underline-offset-4 hover:underline hover:text-link-hover active:text-link-active active:underline cursor-pointer'
})
